# TODO

- Add frontend-model `joins(...)` support using relationship-object descriptors only (reject raw SQL joins).
- Add frontend-model support for `findOrCreateBy(...)` and `findOrInitializeBy(...)` with validated, structured conditions.

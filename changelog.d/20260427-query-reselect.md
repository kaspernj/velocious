- Added `Query#reselect(select)` to replace any previously accumulated `SELECT` clauses with the given ones — mirrors Active Record's `reselect`. Calling `reselect()` with no argument drops the projection so the driver falls back to its default `SELECT *`. Use this to repurpose a base query for an aggregate or a column-projected read without reaching into private `_selects` state.

Frontend-model responses now return structured per-attribute validation errors when a record save fails validation. The response includes `errorType: "validation_error"`, a human-readable `errorMessage`, and a `validationErrors` object keyed by attribute name with `type`, `message`, and `fullMessage` per error. Validation errors are safe to expose in all environments; unexpected internal errors remain masked as "Request failed." in production.

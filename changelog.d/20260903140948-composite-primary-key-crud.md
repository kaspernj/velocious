Add composite primary-key CRUD for backend records and generated frontend models, including key-changing updates, alias-aware attachment list and member authorization, resource-filtered lifecycle serialization, and per-resource lifecycle routing that preserves remote listener identity changes without requiring unloaded alias keys. Composite identities use an exact object whose fields match the configured non-empty, unique primary-key array; unsupported scalar-only features now fail with an explicit error.

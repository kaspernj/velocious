Add composite primary-key CRUD for backend records and generated frontend models, including key-changing updates, resource authorization, backing-record attachment ownership, and per-resource lifecycle event routing. Composite identities use an exact object whose fields match the configured primary-key array; unsupported scalar-only features now fail with an explicit error.

Make `execution_mode` the single source of truth for a background job's runtime: drop the redundant `forked` enqueue-option alias and the `forked` boolean column (a versioned, advisory-locked migration rewrites the pre-existing pooled-as-forked rows to `execution_mode = "pooled"`, clears the queued handoff marker, and drops the column). Pooled jobs now persist as `execution_mode = "pooled"` directly instead of `"forked"` plus a `velocious-pooled:*` handoff sentinel. Select a non-default runtime with `executionMode: "inline" | "forked" | "spawned"`.

Add portable durable stable-schedule readback and wake APIs across Node SQL/TCP and Browser/Expo local SQLite background jobs. The local adapter now supports transactional replacement/cancellation parity, retains keyed history through additive schema-v2 and monotonic-order schema-v3 upgrades, and wakes future retries on the same job row without changing attempt or failure lineage. Stable lookup uses the handoff/terminal count-revision fence, validates complete slot-specific TCP rows, and orders terminal history by transaction-assigned ownership rather than timestamp or random id. Node SQL stores retain a per-key ownership-order high-water mark outside prunable terminal history and backfill it from the greatest retained ordered row during additive upgrade.

## Testing

- Capture console output during each test run and persist failed-test console logs to `tmp/screenshots` alongside other failure assets, then print each log path in the failed test summary.

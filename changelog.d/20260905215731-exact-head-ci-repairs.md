# Fixed

- Keep global testing-configuration hooks out of per-file profile weights and timing manifests while retaining their aggregate setup/cleanup cost.
- Serialize PostgreSQL `character varying` predicate values as strings, matching the existing string-column contract.
- Remove ambient configuration and shared-connection polling races from the affected cross-driver framework specs, and make Git installation coverage refresh registry metadata before resolving released dependencies.

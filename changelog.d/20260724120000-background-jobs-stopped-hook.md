Background job main and worker processes now support an exactly-once `onStopped` lifecycle hook and share one promise across repeated or concurrent `stop()` calls, allowing embedded/test coordinators to clean up externally owned resources without replacing `stop()` or dropping worker shutdown options. The hook still runs after shutdown failures, and dual shutdown/hook failures are reported together with the primary shutdown error first.

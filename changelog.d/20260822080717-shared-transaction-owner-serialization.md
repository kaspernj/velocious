Fixed shared test-transaction serialization so broker requests and async descendants share one physical-connection ownership boundary. Broker entries now drain inherited sibling work, delayed callbacks re-enter the broker after their owner scope ends, and nested owned calls serialize through child FIFOs, preventing concurrent requests from poisoning one-request transaction drivers such as MS-SQL.

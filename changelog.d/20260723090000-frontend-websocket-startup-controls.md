Frontend-model WebSocket connections and channel subscriptions now accept `timeoutMs` and `signal` startup controls. One deadline covers connection and server-confirmed readiness without multiplying the timeout, caller abort reasons are preserved, failed startup cleans up partial handles, and startup timers cannot terminate healthy long-lived subscriptions. Session cancellation also stops reconnect and buffered-event flush work deterministically.

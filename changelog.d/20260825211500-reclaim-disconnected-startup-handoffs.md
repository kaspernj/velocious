Reclaim exact pre-start background-job handoffs whose workers do not successfully reconnect and adopt through a still-live socket within the new main generation's bounded grace, coordinating with adoption queries already in flight at the deadline while bounding stuck-query waits. Use the existing orphan retry/count/concurrency/event lifecycle while fencing adopted workers, post-start leases, and newer re-handoffs. Reject reconnect grace values outside Node's safe timer range instead of overflowing into immediate reclaim.

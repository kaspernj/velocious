Background-jobs dashboards can now establish an authoritative count snapshot
with a durable database revision and subscribe to authorized, database-scoped
WebSocket/Beacon count deltas emitted only after successful commits.

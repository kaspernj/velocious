`Migration#changeTable(tableName, {bulk}, callback)` now applies multiple column, index, and reference changes from a single callback. Compatible operations combine into one bulk `ALTER TABLE` on MySQL/MariaDB/PostgreSQL drivers that opt into bulk support; drivers without bulk support execute the recorded operations sequentially against the real schema, so the same migration source produces the same schema everywhere.
- Remove the callable deployment API (`VelociousDeploymentApi`) introduced in Velocious 1.0.572: the deployment-specific source (`src/deployment-api/`), specs, dummy mounts/adapters, and `docs/deployment-api.md` are gone from the Velocious core. **Migration:** the API moved to `rampway/velocious` and is published in rampway 0.3.1, which owns the deployment integration adapter and execution; mount it from the rampway package instead of `velocious/build/src/deployment-api/index.js`. The shared `bearer-token`/`mount-prefix` utilities remain in Velocious because background-jobs uses them.

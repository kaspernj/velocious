* Add async, connection-aware computed scope metadata for server sync publishing while preserving static declarations.
* Add abort-safe `SyncClient` stop, selected-scope reset, and atomic identity-replacement lifecycles with transactional app-owned cache cleanup.

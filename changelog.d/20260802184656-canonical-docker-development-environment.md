## Added

- Add the canonical root `Dockerfile` and `compose.yml` Docker development setup: an ordinary `dev` service on Ubuntu 26.04 LTS (digest-pinned) with Node.js 24.x from signed NodeSource, the universal apt baseline, and the newest published provider CLIs from bare unversioned npm specs, running as UID/GID 1000:1000 with a single dedicated development-home bind mount (`DEV_HOME_PATH`) plus a read-only GitHub CLI config bind, and standard Compose project naming for concurrent isolated instances. Includes `scripts/docker-run.sh`, `.env.example`, `.dockerignore`, and the checked-in static contract verifier (`npm run verify:docker-dev-environment`). See [docs/docker-development-environment.md](../docs/docker-development-environment.md) and the "Docker development environment" section in `README.md`.

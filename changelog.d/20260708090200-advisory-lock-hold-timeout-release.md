`Record.withAdvisoryLock` and `Record.withAdvisoryLockOrFail` now acquire and release advisory locks through a dedicated lock connection instead of the callback's application connection. This lets `holdTimeoutMs` release the lock before raising `AdvisoryLockHoldTimeoutError`, even when the callback's own database work is still hung.

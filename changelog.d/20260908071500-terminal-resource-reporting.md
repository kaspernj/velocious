Project the shared runner's terminal-resource lifecycle into Velocious reporting: suppress retry announcements, emit `testNotRun`, expose separate not-run counts/details, retain full causal stacks, and keep terminal setup/run exits unsuccessful. Resolve declaration locations correctly through linked shared-runner packages. Requires coordinated adoption of the shared runner's terminal-resource contract.

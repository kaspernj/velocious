Add attempt-scoped transactional tenant connection registration for tests, keyed by logical database identifier and resolved physical tenant configuration, retained through `afterEach` and safely revoked during timed-out lifecycle cleanup.

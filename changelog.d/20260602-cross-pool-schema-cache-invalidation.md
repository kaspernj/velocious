- Invalidate schema metadata caches across every pool that targets the same physical database (matched by connection reuse key) when DDL runs through any one of them. Previously each pool kept an independent schema cache, so a migration/rollback executed through one pool left sibling pools reporting stale tables and columns — which surfaced as `TRUNCATE`/lookup failures against tables that had already been dropped.

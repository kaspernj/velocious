- Add a mountable authenticated callable deployment API (`VelociousDeploymentApi`): allowlisted project/stage targets, full immutable revision validation via the configured adapter, bearer-token authentication with constant-time comparison and redaction, idempotent persisted runs with advisory-lock-fenced creation (consistent deployment-lock then key-lock ordering) and bounded conflicts, owner/status-fenced terminal writes, duplicate-blocking reconciliation for ambiguous running outcomes and post-success persistence failures, ownership-lease heartbeat recovery for pre-execution interruptions, audit persistence that reports failures on the framework-error channel without stranding deployments, sanitized run/audit readback, and adapter-owned execution so integrations such as Rampway reuse their normal lock/build/release/health/rollback/cleanup semantics.

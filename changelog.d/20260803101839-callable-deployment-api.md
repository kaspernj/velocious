- Add a mountable authenticated callable deployment API (`VelociousDeploymentApi`): allowlisted project/stage targets, full immutable revision validation via the configured adapter, bearer-token authentication with constant-time comparison and key/value redaction, mount-scoped idempotent persisted runs with advisory-lock-fenced creation (consistent deployment-lock then key-lock ordering) and bounded conflicts, pending/owner/status-fenced execution and terminal writes, duplicate-blocking reconciliation for ambiguous running outcomes and post-success persistence failures, ownership-lease heartbeat recovery for pre-execution interruptions, audit persistence that reports consumed framework failures on both `framework-error` and unified `all-error` without stranding deployments, sanitized run/audit readback, and adapter-owned execution so integrations such as Rampway reuse their normal lock/build/release/health/rollback/cleanup semantics.

Frontend-model resources can now customize index queries through `indexQuery()`, `countIndexQueryOptions()`, and pagination/search/sort hooks while preserving built-in pluck and aggregate count behavior.

Preserve exact pooled-child shutdown provenance through durable failure reports and events, including typed parent/signal/IPC reasons, request and observation timestamps, stable child identity, and bounded in-flight job IDs; drained retirement now fails loudly if the parent still tracks work and does not re-advertise already-published replacement capacity. The deprecated `terminationReason` category remains available for existing consumers alongside authoritative `shutdownReason`.

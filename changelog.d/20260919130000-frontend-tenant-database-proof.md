Prove frontend tenant SQLite isolation with two real persisted SQL.js databases
in the browser harness, bounded forty-project churn, and an all-platform Expo
fixture that uses public tenant handles and the real platform SQLite driver
instead of an injected connection.

Async-tracked database pools now reap checked-in idle connections after 5 seconds by default, configurable with `pool.idleTimeoutMillis`. Background job workers also cap spawned runners with `maxConcurrentForkedJobs` so out-of-process job bursts cannot create unbounded database-using child processes.

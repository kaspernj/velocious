Refine tenant typing (follow-up to the tenant Record typing): `Current.setTenant()` / `Current.withTenant()` accept `object` so app tenants modeled as an interface or class stay assignable, and the `switchesTenantDatabase(...)` resolver callback receives `Record<string, unknown> | null | undefined` since it can be invoked outside a tenant scope (resolvers must narrow rather than read directly).

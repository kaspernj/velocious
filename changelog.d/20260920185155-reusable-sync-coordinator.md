Added a reusable observable sync coordinator with single-flight replay/realtime/pull ordering, bounded injected retry scheduling, deterministic lifecycle teardown, privacy-safe conflict diagnostics, explicit conflict resolution, generation-fenced online checks, linearizable client tracking start/stop, and fail-loud background error reporting. Replay services can now use application-owned private durable receipts to deduplicate exact lost-response retries even after public feed rows advance. SQL Server test cleanup now owns constraint toggles and truncation in one physical request.

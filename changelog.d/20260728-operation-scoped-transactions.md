Add opt-in singular-database `configuration.withTransaction` operations with operation-bound model/query/record ownership, nested savepoints, non-retried owned after-commit callbacks, cross-database, cross-tenant-physical-database, expired-handle, and pre-owned Single-pool transaction rejection, fresh async-tracked checkouts, exclusive FIFO isolation on shared SQLite/SQL.js connections, and transaction-aware SQL.js persistence.

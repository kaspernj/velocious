- Add `Model.usingTenant(tenant)` returning a scope whose eager finders (`find`, `findBy`, `findByOrFail`) run against an explicit tenant (and therefore its database) instead of the ambient `Current.tenant()`, ensuring that tenant's connections for the duration of each query. Useful for reading a model that may live in a specific tenant database or the default database from another tenant context, without a cross-database scan.

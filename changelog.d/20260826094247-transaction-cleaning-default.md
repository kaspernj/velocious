Default test database-cleaning metadata to transaction rollback across setup, test, and teardown hooks while preserving explicit truncation and no-cleaning opt-outs, leave transaction-disabled non-request tests on independently owned connections after dummy bootstrap, and provision attachment schema during `db:migrate` before transactional tests use it.

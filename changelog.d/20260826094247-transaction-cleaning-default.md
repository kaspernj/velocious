Default test database-cleaning metadata to transaction rollback across setup, test, and teardown hooks while preserving explicit truncation and no-cleaning opt-outs, avoid redundant browser-dummy truncation for transaction-cleaned tests, quarantine browser database-cleaning connections and revoke inherited database access when lifecycles remain active after timeout cleanup, keep persistent Node Dummy infrastructure outside inherited connection contexts and revocable attempt scopes even when `Dummy.run()` is called directly, retain one lifecycle connection for explicit truncation without exposing transaction sharing, serialize coordinated physical connections across complete transaction lifecycles and captured operation-lease admission, leave no-cleaning non-request tests on independently owned connections, report identical detached cleanup failures once, and provision attachment schema during `db:migrate` before transactional tests use it.

Default test database-cleaning metadata to transaction rollback across setup, test, and teardown hooks while preserving explicit truncation and no-cleaning opt-outs, avoid redundant browser-dummy truncation for transaction-cleaned tests, quarantine browser database-cleaning connections and revoke inherited database access when lifecycles remain active after timeout cleanup, leave transaction-disabled non-request tests on independently owned connections after dummy bootstrap, and provision attachment schema during `db:migrate` before transactional tests use it.

The canonical development container now mounts one durable Codex/Kimi/OpenCode provider runtime plus one exact read-only agent-context bundle at `/opt/hermes-agent-context`, validates real runtime parents and canonical credential aliases, bootstraps idempotent discovery links with conflict-preserving migration backups, selects direct provider binaries for Threadwire, and keeps OpenCode lane state local except for shared authentication.

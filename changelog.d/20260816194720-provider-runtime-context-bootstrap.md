The canonical development container now mounts one durable Codex/Kimi/OpenCode provider runtime plus one exact read-only agent-context bundle at `/opt/hermes-agent-context`, rejects overlapping resolved sources, validates real runtime parents and canonical credential aliases, serializes shared discovery-link migration, preserves conflicts in migration backups, selects direct provider binaries for Threadwire, and keeps OpenCode lane state local except for shared authentication.

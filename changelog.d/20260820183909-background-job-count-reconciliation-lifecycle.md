Background-job routine schema and connection readiness no longer rebuild global concurrency counts on every check. The broad count-reconciliation UPDATE now runs through the explicit, advisory-locked `background-jobs-main` startup lifecycle, which logs its database identifier and duration, plus the concrete schema-recovery path that recreates a physically missing `background_jobs` table and must clear surviving stale active counts.

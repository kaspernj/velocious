`db:schema:dump` now includes applied `schema_migrations` rows in the structure SQL output so checked-in snapshots carry the migration ledger alongside the DDL. Loading a structure dump (`db:schema:load`) replays these inserts, marking every listed version as already applied. This prevents schema loads from re-running migrations against a database that already contains the post-migration schema, which would fail with duplicate-table or migration-version errors.

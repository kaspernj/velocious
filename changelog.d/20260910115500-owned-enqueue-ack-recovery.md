Recover a generation-owned background-job enqueue once after an ambiguous post-send acknowledgement failure by replaying its exact existing producer proof and invocation identity. The durable store returns the original job instead of inserting a duplicate; pre-send generation failures, explicit enqueue rejections, and ordinary enqueues retain their existing fail-loud behavior and `idempotencyKey` semantics.

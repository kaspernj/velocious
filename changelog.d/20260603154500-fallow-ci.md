Add a `fallow` codebase-intelligence regression gate. `npm run fallow` checks dead code, duplication, and complexity against committed baselines in `fallow-baselines/` and fails only on new findings; `npm run fallow:baseline` refreshes them. Wired into CI as the `Fallow` build, configured via `.fallowrc.json`.

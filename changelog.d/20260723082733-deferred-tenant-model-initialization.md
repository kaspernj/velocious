Initialize existing deferred tenant model metadata before `Tenant.with` and `Tenant.each` runtime callbacks, sharing in-progress initialization across concurrent tenant entries and routing generated translation classes through their translated model's tenant database resolver. Dummy test configurations now read the shared MSSQL credential from `MSSQL_SA_PASSWORD` instead of hardcoding it.
